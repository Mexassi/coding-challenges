package io.github.mexassi

fun String.isPalindromicAnagram() = palindromicAnagram(this)

private fun palindromicAnagram(string: String): Boolean {
    // push everything into a stack
    // remove each element from the stack when present
    // return true when either 1 element left and string length is a odd number or 0 els left and string length is even

    return true
}